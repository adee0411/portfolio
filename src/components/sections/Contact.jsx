import SectionWrapper from "../../UI/SectionWrapper";

const Contact = () => {
  return (
    <SectionWrapper sectionID="contact" sectionTitle="Kontakt"></SectionWrapper>
  );
};

export default Contact;
